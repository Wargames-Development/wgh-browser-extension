(() => {
  'use strict';

  // Reserved boundary for a possible future Technic update-publisher workflow.
  // Do not advertise or invoke this until Wargames Solder exposes a supported,
  // tested job contract and the browser behavior has been approved separately.

  window.WGH_TECHNIC_UPDATE_PUBLISHER_FUTURE = Object.freeze({
    implemented: false,
    reservedJobType: 'technic_update_publish_future'
  });
})();
